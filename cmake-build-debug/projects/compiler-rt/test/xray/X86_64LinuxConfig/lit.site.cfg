## Autogenerated from /home/nsas2020/ufo/llvm-6.0.0/projects/compiler-rt/test/xray/lit.site.cfg.in
## Do not edit!

# Tool-specific config options.
config.name_suffix = "-x86_64-linux"
config.xray_lit_source_dir = "/home/nsas2020/ufo/llvm-6.0.0/projects/compiler-rt/test/xray"
config.target_cflags = "-m64"
config.target_arch = "x86_64"
config.built_with_llvm = ("" != "TRUE")

# TODO: Look into whether we can run a capability test on the standalone build to
# see whether it can run 'llvm-xray convert' instead of turning off tests for a
# standalone build.
if config.built_with_llvm:
  config.available_features.add('built-in-llvm-tree')

# Load common config for all compiler-rt lit tests
lit_config.load_config(config, "/home/nsas2020/ufo/llvm-6.0.0/cmake-build-debug/projects/compiler-rt/test/lit.common.configured")

# Load tool-specific config that would do the real work.
lit_config.load_config(config, "/home/nsas2020/ufo/llvm-6.0.0/projects/compiler-rt/test/xray/lit.cfg")
