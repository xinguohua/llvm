## Autogenerated from /home/nsas2020/ufo/llvm-6.0.0/projects/compiler-rt/test/tsan/Unit/lit.site.cfg.in
## Do not edit!

# Load common config for all compiler-rt unit tests.
lit_config.load_config(config, "/home/nsas2020/ufo/llvm-6.0.0/cmake-build-debug/projects/compiler-rt/unittests/lit.common.unit.configured")

# Setup config name.
config.name = 'ThreadSanitizer-Unit'

# Setup test source and exec root. For unit tests, we define
# it as build directory with ASan unit tests.
# FIXME: De-hardcode this path.
config.test_exec_root = "/home/nsas2020/ufo/llvm-6.0.0/cmake-build-debug/projects/compiler-rt/lib/tsan/tests"
config.test_source_root = config.test_exec_root

if config.host_os == 'Darwin':
  config.parallelism_group = config.darwin_sanitizer_parallelism_group_func

  # On Darwin, we default to ignore_noninstrumented_modules=1, which also
  # suppresses some races the tests are supposed to find.  See tsan/lit.cfg.
  if 'TSAN_OPTIONS' in config.environment:
    config.environment['TSAN_OPTIONS'] += ':ignore_noninstrumented_modules=0'
  else:
    config.environment['TSAN_OPTIONS'] = 'ignore_noninstrumented_modules=0'
